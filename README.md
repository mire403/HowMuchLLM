# HowMuchLLM
HowMuchLLM 是一个 Python CLI 工具，用于估算 LLM 应用的真实调用成本。输入 prompt tokens、completion tokens 和每月调用次数，即可对比主流模型的月度成本，并输出 Markdown 表格。 核心功能 对比 OpenAI、Anthropic、Google、Meta 等主流模型的成本差异 支持 YAML 配置，一行命令生成成本报告 输出标准 Markdown 表格，便于集成到文档 适用场景 产品经理、开发者和财务团队进行模型选型和预算规划。
